kind: PersistentVolumeClaim
apiVersion: v1
metadata: 
    name: mysql-pvc
    namespace: bankapp
spec:
    storageClassName: manual
    resources:
        requests:
            storage: 5Gi
    accessModes:
     - ReadWriteOnce
    selector:
        matchLabels:
            app: mysql

